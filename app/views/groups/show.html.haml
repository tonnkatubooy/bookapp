.group_name
  = @group.name
  %h2 メンバー一覧
  .memberLists
    - @group.users.each do |user|
      = user.name
  .message_form    
    = form_for @group do |f|
      = f.text_field :name, class: "chat-group-form__input", placeholder: "グループ名を入力してください"